# Parse subsidy date fields

Handles character dates that may come from Excel as serial numbers,
POSIXct text, or standard date strings. Identical logic to program date
parsing.

## Usage

``` r
.parse_subsidy_date(x)
```

## Arguments

- x:

  Character vector of date values

## Value

Date vector
