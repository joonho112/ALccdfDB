# Check if a suggested package is available

Check if a suggested package is available

## Usage

``` r
.check_suggested(pkg, reason = NULL)
```

## Arguments

- pkg:

  Character string package name

- reason:

  Why the package is needed
