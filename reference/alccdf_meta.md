# Extract metadata from an ALccdfDB S3 object

Extract metadata from an ALccdfDB S3 object

## Usage

``` r
alccdf_meta(obj)
```

## Arguments

- obj:

  An ALccdfDB S3 object

## Value

A list of metadata
