# Format a count for display

Format a count for display

## Usage

``` r
.msg_count(n, label, .envir = parent.frame())
```

## Arguments

- n:

  Numeric count

- label:

  Label for the count

- .envir:

  Environment for cli glue evaluation
