# Export linked clients-programs data to RDS

Export linked clients-programs data to RDS

## Usage

``` r
linkage_clients_programs_export_rds(obj, path, verbose = TRUE)
```

## Arguments

- obj:

  An `alccdf_linked_clients_programs` object

- path:

  Output file path

- verbose:

  Logical; print progress messages? Default TRUE.

## Value

Invisible file path
