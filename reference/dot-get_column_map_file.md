# Get the path to the appropriate column map CSV

Get the path to the appropriate column map CSV

## Usage

``` r
.get_column_map_file(type, format_version)
```

## Arguments

- type:

  Program type

- format_version:

  `"legacy"` or `"current"`

## Value

Full path to the CSV file within `inst/extdata/mappings/`
