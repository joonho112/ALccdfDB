# Export linked programs-enrolled data to RDS

Export linked programs-enrolled data to RDS

## Usage

``` r
linkage_programs_enrolled_export_rds(obj, path, verbose = TRUE)
```

## Arguments

- obj:

  An `alccdf_linked_programs_enrolled` object

- path:

  Output file path

- verbose:

  Logical; print progress messages? Default TRUE.

## Value

Invisible file path
