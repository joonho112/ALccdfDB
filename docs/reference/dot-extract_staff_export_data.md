# Extract data for export, optionally including PII

Extract data for export, optionally including PII

## Usage

``` r
.extract_staff_export_data(obj, include_pii = FALSE)
```

## Arguments

- obj:

  A staff S3 object or data frame

- include_pii:

  Logical; whether to join PII fields

## Value

A tibble
