# Program Module: Clean

Clean and standardise raw program data. Handles address standardisation,
date/time parsing, capacity conversion, age-range extraction, tier
recoding, factor level assignment, and derived-variable computation
matching the established cleaning protocol from
`all_programs_appended_cleaned`.
