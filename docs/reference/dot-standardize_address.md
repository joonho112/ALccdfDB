# Standardize an address string

Applies the address standardization pipeline documented in the existing
analysis scripts. Handles common abbreviations, directionals, and
formatting.

## Usage

``` r
.standardize_address(addr)
```

## Arguments

- addr:

  Character vector of address strings

## Value

Character vector of standardized addresses
