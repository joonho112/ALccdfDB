# Export linked programs-staff data to Parquet

Export linked programs-staff data to Parquet

## Usage

``` r
linkage_programs_staff_export_parquet(obj, path, verbose = TRUE)
```

## Arguments

- obj:

  An `alccdf_linked_programs_staff` object

- path:

  Output file path

- verbose:

  Logical; print progress messages? Default TRUE.

## Value

Invisible file path
