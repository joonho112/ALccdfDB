# Parse program date fields

Handles character dates that may come from Excel as serial numbers,
POSIXct text, or standard date strings.

## Usage

``` r
.parse_program_date(x)
```

## Arguments

- x:

  Character vector of date values

## Value

Date vector
