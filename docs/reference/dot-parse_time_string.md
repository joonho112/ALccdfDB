# Parse time string to hms-compatible format

Parse time string to hms-compatible format

## Usage

``` r
.parse_time_string(time_str)
```

## Arguments

- time_str:

  Character vector of time strings

## Value

Character vector in HH:MM:SS format
