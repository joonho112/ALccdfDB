# Display a warning message

Display a warning message

## Usage

``` r
.msg_warn(msg, .envir = parent.frame())
```

## Arguments

- msg:

  Character string message (supports cli glue syntax)

- .envir:

  Environment for cli glue evaluation
